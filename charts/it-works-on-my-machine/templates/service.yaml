apiVersion: v1
kind: Service
metadata:
  name: {{ include "it-works-on-my-machine.fullname" . }}
  labels:
    {{- include "it-works-on-my-machine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "it-works-on-my-machine.selectorLabels" . | nindent 4 }}
