apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "it-works-on-my-machine.fullname" . }}-test-connection"
  labels:
    {{- include "it-works-on-my-machine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "it-works-on-my-machine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
