# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.

# VCS directories
.git/
.gitignore
.github/
.bzr/
.bzrignore
.hg/
.hgignore
.svn/

# Common backup and temporary files
*.swp
*.bak
*.tmp
*.orig
*~
*.log

# Various IDEs
.DS_Store
.project
.idea/
*.tmproj
.vscode/

# Build artifacts
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage/
dist/
build/

# Documentation (if you don't want it in the chart)
README.md
CHANGELOG.md

# CI/CD files
.github/
.gitlab-ci.yml
Jenkinsfile
.travis.yml

# Docker files (not needed in chart)
Dockerfile
.dockerignore

# Development files
.env
.env.local
.env.development
.env.test
.env.production

# Chart development files
Chart.lock
values-*.yaml
!values.yaml
