{
  "name": "it-works-on-my-machine",
  "version": "1.0.0-alpha.11",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --config config/jest.config.js --runInBand",
    "test:watch": "jest --config config/jest.config.js --watch --runInBand",
    "test:coverage": "jest --config config/jest.config.js --coverage --runInBand",
    "test:unit": "jest --config config/jest.config.js tests/unit --runInBand",
    "test:integration": "jest --config config/jest.config.js tests/integration --runInBand",
    "test:edge": "jest --config config/jest.config.js tests/integration/edge-cases.test.js --runInBand",
    "test:verbose": "jest --config config/jest.config.js --verbose --runInBand"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "conventional-changelog-conventionalcommits": "^9.1.0",
    "express": "^4.18.2",
    "lint": "^1.1.2"
  },
  "devDependencies": {
    "aws-sdk": "^2.1412.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.4"
  }
}
